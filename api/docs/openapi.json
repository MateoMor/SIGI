{
  "openapi": "3.0.1",
  "info": {
    "title": "SIGI API (static docs)",
    "version": "1.0.0",
    "description": "Documento OpenAPI mínimo generado como fallback estático porque @nestjs/swagger no es compatible con la versión actual de Nest en este proyecto."
  },
  "servers": [
    { "url": "http://localhost:3000", "description": "Local" }
  ],
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  },
  "paths": {
    "/": {
      "get": {
        "summary": "Health check",
        "responses": {
          "200": {
            "description": "OK"
          }
        }
      }
    }
  }
}
